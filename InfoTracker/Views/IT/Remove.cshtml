<h1>@ViewBag.title</h1>
<form method="post">
    @foreach (var it in ViewBag.ITs)
    {
        <input type="checkbox" name="ITIds" id="@it.ID" value="@it.ID" />
        <label for="@it.ID">@it.Name</label>
        <br />
    }
    <input type="submit" value="Remove" />
</form>